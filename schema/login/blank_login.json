{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id":  "blank_login",
    "type": "object",
    "title": "Schema for a login with blank fields",
    "anyOf": [
        {"required": ["password"]},
        {"required": ["email"]}
    ],
    "minProperties": 1,
    "maxProperties": 2,
    "properties": {
        "email": {
            "type": "string",
            "default": "email não pode ficar em branco",
            "title": "The email Schema",
            "examples": [
                "email não pode ficar em branco"
            ]
        },
        "password": {
            "type": "string",
            "default": "password não pode ficar em branco",
            "title": "The password Schema",
            "examples": [
                "password não pode ficar em branco"
            ]
        }
    },
    "examples": [{
        "message": "Cadastro realizado com sucesso",
        "_id": "XuFCZFtsZ7bsRUIh"
    }]
}